# Desktop app (desktop/app.py). Installed into backend/.venv by
# scripts/desktop.bat on Windows and scripts/desktop.sh on macOS/Linux.
# pywebview and pystray pull the Windows (pythonnet) or macOS (pyobjc) backend
# automatically from the platform markers, so this one file serves every OS.
-r requirements.txt

# Pure Python threaded WSGI server: serves the API and the SPA locally.
waitress==3.0.2
# Native window on top of Edge WebView2 (Windows) or WKWebView (macOS).
pywebview==6.2.1
# Tray / menu-bar icon that keeps the monitor alive with the window closed.
pystray==0.19.5
# Draws the app icon for the tray and the .ico/.icns files.
pillow==12.3.0
